## What's new in config-as-code

- Environment configurations updated for the **niuatt EKS cluster**
- SOPS encryption configuration aligned with the new AWS KMS key
- Environment value files organized and cleaned up — legacy configs moved to `environments_old/`
- Helm charts updated to match the upgraded microservice versions deployed in niuatt
